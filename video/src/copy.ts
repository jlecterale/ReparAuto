/**
 * Locale of a reel's copy. The product ships in Portugal (pt — European
 * Portuguese, "tu") and Brazil (br — Brazilian Portuguese, "você"), and ad
 * copy that mixes the two sounds off in both markets, so every reel builds
 * its scenes from a per-locale copy table.
 */
export type Locale = "pt" | "br";
